%nav.top-bar{"data-topbar" => ""}
  %ul.title-area
    %li.name
      %h1
        = link_to "La frabique", root_path
    %li.toggle-topbar.menu-icon
      %a{href: "#"} Men
  %section.top-bar-section
    / Right Nav Section
    %ul.right
      %li.active
      -if current_user
        =link_to "Log out", destroy_user_session_path, :method => :delete
      -else
        =link_to "Log in", user_session_path
      
    / Left Nav Section